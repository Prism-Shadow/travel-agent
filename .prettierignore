# build output and dependencies
node_modules/
dist/
pnpm-lock.yaml

# Agent scratch: git-ignored by its own nested .gitignore, which Prettier does not read
# (Prettier consults the root .gitignore only), so a working file there would fail format:check.
.thinkrail/

# docs-only area (mostly Chinese; not machine-formatted)
specs/
*.md

# Playwright e2e artifacts
test-results/
playwright-report/

# Vendored penguin-browser: keeps its own formatting. Checking it against this
# repo's Prettier would rewrite ~200 files on every CI run.
packages/browser-cli/
packages/browser-extension/
packages/desktop/dist-e2e/
# Vendored Amap skill (amap-web/amap-lbs-skill): keeps its own formatting, like the browser packages
packages/skills/skills/amap-lbs-skill/
# electron-builder staging tree (git-ignored build artifact; contains copied bundles)
packages/desktop/stage/
