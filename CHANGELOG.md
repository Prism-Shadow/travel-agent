# Changelog

One brief line per release. Per-release detail lives in [`changelog/<version>/`](changelog/).

- No releases have been cut from this fork yet. Ongoing work accumulates in [`changelog/unreleased/`](changelog/unreleased/).

This repo hard-forked PenguinHarness at its 0.2.2 baseline (`d14be6f`). Upstream release history (0.0.1 – 0.2.2) was removed from the tree and survives only in git history.
