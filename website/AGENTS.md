# Website working instructions

The user's current instruction is local-only development and preview. Do not create, push,
publish, or redeploy a hosted website unless the user explicitly requests deployment in a later
message. This applies to private previews as well as public sites.

Preserve the local website source. The existing Sites project is pending removal; its ID in
`.openai/hosting.json` identifies that cleanup target and is not authorization to deploy.
See [the working ledger](../tasks/website-launch.md) for the removal status.
