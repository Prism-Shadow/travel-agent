# Copy to .env (ignored by .gitignore) and fill in real values for local runs and e2e.
# e2e picks a Provider by available key, in order: Claude (ANTHROPIC_API_KEY) -> DeepSeek (DEEPSEEK_API_KEY).
ANTHROPIC_API_KEY=
# Optional: custom Claude gateway URL (defaults to the official Anthropic endpoint if unset).
# ANTHROPIC_BASE_URL=
# DeepSeek (CI's e2e uses this key, model deepseek-v4-flash).
DEEPSEEK_API_KEY=
# Optional: custom DeepSeek gateway URL (defaults to the official https://api.deepseek.com).
# DEEPSEEK_BASE_URL=

# Optional destination suggestions. Photon/OpenStreetMap is the default; point this at a
# self-hosted Photon `/api/` endpoint, or turn all destination lookup traffic off.
# PENGUIN_GEOCODER_URL=https://photon.komoot.io/api/
# PENGUIN_LOCATION_SEARCH=off
