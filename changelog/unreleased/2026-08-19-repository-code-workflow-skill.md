# Repository code workflow skill

The code workflow is now available as an explicit, project-local Codex skill with valid discovery metadata.

## Details

- Add `.agents/skills/code-workflow/SKILL.md` so the workflow travels with this repository.
- Give the skill a focused description for discovery and selection.
- Preserve its explicit-only behavior through `agents/openai.yaml`.
