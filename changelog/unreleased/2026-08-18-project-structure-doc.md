# Docs: current project structure map

Added a source-oriented directory tree under `docs/project-structure/` so contributors can quickly see what each workspace package and major folder owns.

The document focuses on current tracked source and configuration paths, and intentionally calls generated directories out without expanding them.
